# S2.6 — Retrieval Benchmark & Evaluation Harness (Task Summary)

**Task:** BARQ G4 · Sprint 2 (S2.6)
**Objective:** Automated harness to quantitatively benchmark the retrieval pipeline against ground-truth incidents and negative controls.
**Status:** ✅ Complete — calibrated 2026-09-19 on real embeddings.

---

## 1. Scope Coverage

| Scope Item | Status |
|---|---|
| Structured dataset, ≥10 answerable (query + expected articles) | ✅ 20 answerable in `benchmark_dataset.json` (incl. 5 paraphrase/typo variants) |
| Structured dataset, ≥3 negative controls | ✅ 10 negatives (5 far-out-of-domain + 5 IT-adjacent near-miss) |
| Single-command runner | ✅ `uv run python benchmark/run_benchmark.py [--real]` |
| Aggregate metrics: Hit Rate + Refusal Correctness | ✅ Printed + saved to `benchmark_results.json` |
| Per-incident output (pass/fail, scores, expected vs retrieved) | ✅ Console log + `answerable_details` / `negative_details` |
| Score-distribution analysis + threshold recommendation | ✅ `RETRIEVAL_EVALUATION.md` §4; threshold sweep in results JSON |

## 2. Files

| File | Purpose |
|---|---|
| `benchmark_dataset.json` | 30 cases: incident text, category, expected articles, control notes |
| `run_benchmark.py` | Runner: incident-text queries, ungated retrieval + Python gate, threshold sweep |
| `benchmark_results.json` | Execution output: provenance, metrics, distributions, per-case detail |
| `RETRIEVAL_EVALUATION.md` | Full report: methodology, results, threshold justification |
| `S2.6_TASK.md` | This file: task-to-deliverable map |

## 3. How to Run

```bash
# Offline plumbing check (SHA-256 stub — validates the harness, not quality)
uv run python benchmark/run_benchmark.py
# Real evaluation (Gemini gemini-embedding-001, requires LITELLM_API_KEY + 768-dim collection)
uv run python benchmark/run_benchmark.py --real
# Custom threshold / top-k
uv run python benchmark/run_benchmark.py 0.65 --real --top-k 5
```

Methodology notes: each case is queried with the **incident text**
(`short_description + description`, the runtime input), filtered by its
`category` (payload-indexed keyword), retrieved ungated (`threshold=0.0`) so
ranking is recorded even below the bar, then gated in Python. The 0.50–0.90
sweep is recomputed from the same pass without re-querying.

## 4. Headline Results (real embeddings, 2026-09-19)

Setup: 33 ServiceNow articles → 211 chunks → Gemini 768-dim vectors in `kb_chunks`.

| Metric @ 0.65 | Result |
|---|---|
| Hit Rate | **95.0%** (19/20) |
| Refusal Correctness | **100.0%** (10/10) |
| Ranking accuracy (threshold ignored) | 95.0% (19/20) |

Distributions: answerable 0.5574–0.8930 (avg 0.7607); negative 0.5261–0.6396
(avg 0.5892). The single miss (`INC-BENCH-009`, 0.5574) ranked the wrong article
and is refused rather than served — the safe failure mode. The pre-fix 0.75
threshold scored only 55% hit and is retired.

## 5. Deliverable Checklist (PR)

- [x] `benchmark_dataset.json` — structured data
- [x] `run_benchmark.py` — runnable script
- [x] `benchmark_results.json` — granular + aggregate execution output
- [x] `RETRIEVAL_EVALUATION.md` — threshold recommendation + rationale
